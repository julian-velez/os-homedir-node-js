const os = require('node:os') // Importa el módulo 'os' de Node.js, que proporciona información del sistema operativo.

console.log(os.homedir()) // Llama al método 'homedir()' del módulo 'os', que devuelve el directorio principal del usuario actual.
